<UserControl x:Class="EquipmentStatusTracker.WPF.Controls.EquipmentControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:EquipmentStatusTracker.WPF.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="80"
             x:Name="Root"
             Cursor="Hand">
    
    <UserControl.Resources>
        <conv:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <conv:StatusToBackgroundConverter x:Key="StatusToBackgroundConverter"/>
        <conv:EquipmentTypeToIconConverter x:Key="EquipmentTypeToIconConverter"/>
    </UserControl.Resources>
    
    <Grid Width="{Binding Equipment.Width, ElementName=Root}" 
          Height="{Binding Equipment.Height, ElementName=Root}"
          ClipToBounds="False">
        <!-- Equipment Shape -->
        <Border x:Name="ShapeBorder" CornerRadius="8"
                Background="{Binding Equipment.Status, ElementName=Root, Converter={StaticResource StatusToBackgroundConverter}}"
                BorderBrush="{Binding Equipment.Status, ElementName=Root, Converter={StaticResource StatusToColorConverter}}"
                BorderThickness="3">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="12" Opacity="0.4" ShadowDepth="3"/>
            </Border.Effect>
            
            <!-- Icon -->
            <TextBlock Text="{Binding Equipment.Type, ElementName=Root, Converter={StaticResource EquipmentTypeToIconConverter}}"
                       FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"
                       Foreground="{Binding Equipment.Status, ElementName=Root, Converter={StaticResource StatusToColorConverter}}"/>
        </Border>
        
        <!-- Status Indicator -->
        <Ellipse Width="18" Height="18" 
                 HorizontalAlignment="Right" VerticalAlignment="Top"
                 Margin="0,-6,-6,0"
                 Fill="{Binding Equipment.Status, ElementName=Root, Converter={StaticResource StatusToColorConverter}}"
                 Stroke="#FF0A0E14" StrokeThickness="3">
            <Ellipse.Effect>
                <DropShadowEffect Color="Black" BlurRadius="6" Opacity="0.5" ShadowDepth="1"/>
            </Ellipse.Effect>
        </Ellipse>
        
        <!-- Label -->
        <Border Background="#CC0A0E14" CornerRadius="4" Padding="6,2"
                HorizontalAlignment="Center" VerticalAlignment="Bottom"
                Margin="0,0,0,-24">
            <TextBlock Text="{Binding Equipment.Name, ElementName=Root}" 
                       FontSize="11" FontFamily="Consolas"
                       Foreground="#FF8B949E"/>
        </Border>
    </Grid>
</UserControl>

